services:
  server:
    image: ghcr.io/mgm103/go-fund-yourself-fe:latest
    container_name: go-fund-yourself-fe
    ports:
      - '3000:3000'
    environment:
      - origin=http://gofundyourself.cash:3000
    volumes:
      - sqlite-data:/app/data

volumes:
  sqlite-data:
